import products from "./mockdata";


// This is the ProductList reducer which we are writing we have to write diffrent reducer for each state in store otherwise it will be very unmaintanble to manage states for whole application in one reducer


// Creating the reducer function for the cartItems
 export default function ProductReducer(state = products, action) {
    
    return state;
}