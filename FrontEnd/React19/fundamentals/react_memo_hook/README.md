## Need of ReactMemo hook : So assume we have a application app component uske andar humne kuch bhi kaam kar rahe hai xyz assume krlo counter app hee bana rakha hai jisme h1 hai button hai incremenet and decrement and and component ko call krre hai and woh component static component hai kuch dynamic kaam nahi ho raha uske andar but important bhi hai so jab hum app component me increase yah decrease par click krenge toh pura app component re-render hoga pura reconcillation process hoga virtual dom create hoga diffing process chalgea diffrences detect kije jaayenge fhir react dom ko diye jaayege reactdom real dom me changes karega fhir purane virtual dom se new virtual dom replace kiya jaayega and app component me jo static component tha woh bhi call hoga static value vaala lekin uski jrurat nahi hai dubara call krne ki . So agar apke component ke andar kuch aise components hai jo aapka main component re-render ho toh aap chahte ho woh call naah ho toh yaha is cheez ko fulfill karta hai reactMemo hook.


## Working of reactMemo hook--->So agar aap koi bigger component banaya hai and usme multiple components banaye hai heavy heavy but yeh multiople components apne parent component par depand hee nahi hai matlb jab parent component re-render ho hum nahi chahe yeh multiple components re-render ho toh un components ko react.memo hook ke callback me daaldo.

Assume hum parent component se child component m kuch props paas krre hai react.memo vaale me use krre hai us props ko toh agar static props hai toh woh child component nahi re-render honge agar dynamic hue toh re-render honge this is how the rect.memo hook works behind the scens.